# External cloned repos (read-only references; not our code)
external/

# Downloaded paper PDFs (large; keep notes.md in git, not the binaries)
references/pdfs/

# Data, checkpoints, eval outputs — live on the Modal Volume in practice
data/
results/*
!results/reanalysis/
!results/figs/
checkpoints/
save/
*.ckpt
*.safetensors

# Secrets — never commit tokens
.env
*.secret
hf_token*

# Inspect eval logs (multi-MB; regenerate or fetch from the Modal Volume)
*.eval

# LaTeX build artifacts
*.aux
*.log
*.out
*.synctex.gz
*.fdb_latexmk
*.fls

# Local tool / IDE state (never commit)
.claude/
CLAUDE.md
.vscode/
.idea/

# Python
__pycache__/
*.py[cod]
.venv/
.venv-eval/
venv/
*.egg-info/
.ipynb_checkpoints/

# OS
.DS_Store
